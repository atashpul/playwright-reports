# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: specs\Smoke\3-HomeDashboardSmoke.spec.ts >> Home Dashboard >> Verify chart title changes with selected tab
- Location: specs\Smoke\3-HomeDashboardSmoke.spec.ts:79:5

# Error details

```
TimeoutError: locator.waitFor: Timeout 20000ms exceeded.
Call log:
  - waiting for locator('//span[text()="Home"]') to be visible

```

# Page snapshot

```yaml
- generic [active] [ref=e1]:
  - main [ref=e4]:
    - heading [level=1] [ref=e6]
    - generic [ref=e9]:
      - generic [ref=e11]:
        - generic: Email *
        - textbox "Email Address" [ref=e12]: pjakati28+PP@gmail.com
      - generic [ref=e14]:
        - generic: Password *
        - textbox "Password" [ref=e15]: Password123!
      - button "Login to Positive Pay" [ref=e17] [cursor=pointer]
      - link "Forgot Password?" [ref=e19]:
        - /url: /forgot-password/
  - heading [level=5] [ref=e21]:
    - text: Could not reconnect to the server.
    - link "Reload" [ref=e22]:
      - /url: ""
    - text: the page to restore functionality.
```

# Test source

```ts
  1   | import { test, expect } from '../../utility/fixture';
  2   | import Constants from '../../utility/Constants';
  3   | 
  4   | test.describe('Home Dashboard', () => {
  5   | 
  6   |     test.beforeEach('Login to Positive Pay', async ({ pageManager }) => {
  7   |         await pageManager.loginPage.navigateToLoginPage('/auth/login');
  8   |         await pageManager.loginPage.login(
  9   |             Constants.USER_FI_EMAIL,
  10  |             Constants.USER_FI_PASSWORD
  11  |         );
  12  |     });
  13  | 
  14  |     // ── Page load ──────────────────────────────────────────────────────────────
  15  | 
  16  |     test('Verify dashboard loads after successful login', async ({ pageManager }) => {
  17  |         await expect(pageManager.homePage.homePageLogo).toBeVisible({ timeout: 20000 });
  18  |         await expect(pageManager.homePage.combinedViewTab).toBeVisible();
  19  |         await expect(pageManager.homePage.exceptionsChartHeading).toBeVisible();
  20  |     });
  21  | 
  22  |     test('Verify all three dashboard view tabs are visible', async ({ pageManager }) => {
  23  |         await pageManager.homePage.homePageLogo.waitFor({ state: 'visible', timeout: 20000 });
  24  |         await expect(pageManager.homePage.combinedViewTab).toBeVisible();
  25  |         await expect(pageManager.homePage.checkOnlyTab).toBeVisible();
  26  |         await expect(pageManager.homePage.achOnlyTab).toBeVisible();
  27  |     });
  28  | 
  29  |     // ── Dashboard chart headings ───────────────────────────────────────────────
  30  | 
  31  |     test('Verify Combined View shows all chart sections', async ({ pageManager }) => {
  32  |         await pageManager.homePage.homePageLogo.waitFor({ state: 'visible', timeout: 20000 });
  33  |         await pageManager.homePage.clickCombinedViewTab();
  34  | 
  35  |         await expect(pageManager.homePage.exceptionsChartHeading).toBeVisible();
  36  |         await expect(pageManager.homePage.exceptionsByDecisionHeading).toBeVisible();
  37  |         await expect(pageManager.homePage.exceptionsByReturnReasonHeading).toBeVisible();
  38  |     });
  39  | 
  40  |     test('Verify Check Only tab shows Check-specific chart sections', async ({ pageManager }) => {
  41  |         await pageManager.homePage.homePageLogo.waitFor({ state: 'visible', timeout: 20000 });
  42  |         await pageManager.homePage.clickCheckOnlyTab();
  43  | 
  44  |         await expect(pageManager.homePage.exceptionsChartHeading).toBeVisible();
  45  |         await expect(pageManager.homePage.exceptionsByReasonHeading).toBeVisible();
  46  |     });
  47  | 
  48  |     test('Verify ACH Only tab shows ACH-specific chart sections', async ({ pageManager }) => {
  49  |         await pageManager.homePage.homePageLogo.waitFor({ state: 'visible', timeout: 20000 });
  50  |         await pageManager.homePage.clickACHOnlyTab();
  51  | 
  52  |         await expect(pageManager.homePage.exceptionsChartHeading).toBeVisible();
  53  |         await expect(pageManager.homePage.exceptionsBySECCodeHeading).toBeVisible();
  54  |     });
  55  | 
  56  |     test('Verify dashboard tab checked state updates correctly', async ({ pageManager }) => {
  57  |         await pageManager.homePage.homePageLogo.waitFor({ state: 'visible', timeout: 20000 });
  58  | 
  59  |         await expect(pageManager.homePage.combinedViewCheckbox).toBeChecked();
  60  |         await expect(pageManager.homePage.checkOnlyCheckbox).not.toBeChecked();
  61  |         await expect(pageManager.homePage.achOnlyCheckbox).not.toBeChecked();
  62  | 
  63  |         await pageManager.homePage.clickCheckOnlyTab();
  64  |         await expect(pageManager.homePage.checkOnlyCheckbox).toBeChecked();
  65  |         await expect(pageManager.homePage.combinedViewCheckbox).not.toBeChecked();
  66  |         await expect(pageManager.homePage.achOnlyCheckbox).not.toBeChecked();
  67  | 
  68  |         await pageManager.homePage.clickACHOnlyTab();
  69  |         await expect(pageManager.homePage.achOnlyCheckbox).toBeChecked();
  70  |         await expect(pageManager.homePage.combinedViewCheckbox).not.toBeChecked();
  71  |         await expect(pageManager.homePage.checkOnlyCheckbox).not.toBeChecked();
  72  | 
  73  |         await pageManager.homePage.clickCombinedViewTab();
  74  |         await expect(pageManager.homePage.combinedViewCheckbox).toBeChecked();
  75  |         await expect(pageManager.homePage.checkOnlyCheckbox).not.toBeChecked();
  76  |         await expect(pageManager.homePage.achOnlyCheckbox).not.toBeChecked();
  77  |     });
  78  | 
  79  |     test('Verify chart title changes with selected tab', async ({ pageManager }) => {
> 80  |         await pageManager.homePage.homePageLogo.waitFor({ state: 'visible', timeout: 20000 });
      |                                                 ^ TimeoutError: locator.waitFor: Timeout 20000ms exceeded.
  81  | 
  82  |         await pageManager.homePage.clickCombinedViewTab();
  83  |         await expect(pageManager.homePage.exceptionsChartHeadingCombined).toBeVisible();
  84  | 
  85  |         await pageManager.homePage.clickCheckOnlyTab();
  86  |         await expect(pageManager.homePage.exceptionsChartHeadingCheckOnly).toBeVisible();
  87  | 
  88  |         await pageManager.homePage.clickACHOnlyTab();
  89  |         await expect(pageManager.homePage.exceptionsChartHeadingACHOnly).toBeVisible();
  90  |     });
  91  | 
  92  |     test('Verify tab-specific charts are hidden for opposite tab', async ({ pageManager }) => {
  93  |         await pageManager.homePage.homePageLogo.waitFor({ state: 'visible', timeout: 20000 });
  94  | 
  95  |         await pageManager.homePage.clickCheckOnlyTab();
  96  |         await expect(pageManager.homePage.exceptionsBySECCodeHeading).toBeHidden();
  97  | 
  98  |         await pageManager.homePage.clickACHOnlyTab();
  99  |         await expect(pageManager.homePage.exceptionsByReasonHeading).toBeHidden();
  100 |     });
  101 | 
  102 |     // ── Summary stats ──────────────────────────────────────────────────────────
  103 | 
  104 |     test('Verify Total Exceptions and Total Exception Amount labels are visible', async ({ pageManager }) => {
  105 |         await pageManager.homePage.homePageLogo.waitFor({ state: 'visible', timeout: 20000 });
  106 |         await expect(pageManager.homePage.totalExceptionsLabel).toBeVisible();
  107 |         await expect(pageManager.homePage.totalExceptionAmountLabel).toBeVisible();
  108 |     });
  109 | 
  110 |     test('Verify summary KPI values have expected number formats', async ({ pageManager }) => {
  111 |         await pageManager.homePage.homePageLogo.waitFor({ state: 'visible', timeout: 20000 });
  112 | 
  113 |         await expect(pageManager.homePage.totalExceptionsValue).toHaveText(/^[0-9,]+$/);
  114 |         await expect(pageManager.homePage.totalExceptionAmountValue).toHaveText(/^\$[0-9,]+(\.[0-9]{2})?$/);
  115 |     });
  116 | 
  117 |     test('Verify delayed data note and force refresh link are visible on dashboard', async ({ pageManager, page }) => {
  118 |         await pageManager.homePage.homePageLogo.waitFor({ state: 'visible', timeout: 20000 });
  119 | 
  120 |         await expect(pageManager.homePage.dashboardDelayNote).toBeVisible();
  121 |         await expect(pageManager.homePage.forceRefreshLink).toBeVisible();
  122 | 
  123 |         const currentUrl = page.url();
  124 |         await pageManager.homePage.forceRefreshLink.click();
  125 |         await expect(page).toHaveURL(currentUrl);
  126 |     });
  127 | 
  128 |     test('Verify dashboard footer footnote and privacy link are visible', async ({ pageManager }) => {
  129 |         await pageManager.homePage.homePageLogo.waitFor({ state: 'visible', timeout: 20000 });
  130 |         await pageManager.homePage.scrollFooterIntoView();
  131 | 
  132 |         await expect(pageManager.homePage.footerEnvironmentTag).toBeVisible();
  133 |         await expect(pageManager.homePage.footerCopyrightText).toBeVisible();
  134 |         await expect(pageManager.homePage.footerAllRightsReservedText).toBeVisible();
  135 |         await expect(pageManager.homePage.footerPrivacyPolicyLink).toBeVisible();
  136 |         await expect(pageManager.homePage.footerPrivacyPolicyLink).toHaveAttribute('href', /PrivacyPolicy/i);
  137 |     });
  138 | 
  139 |     test('Verify profile menu shows My Info and Logout links', async ({ pageManager }) => {
  140 |         await pageManager.homePage.homePageLogo.waitFor({ state: 'visible', timeout: 20000 });
  141 | 
  142 |         await pageManager.homePage.openUserProfileMenu();
  143 |         await expect(pageManager.homePage.myInfoMenuItem).toBeVisible();
  144 |         await expect(pageManager.homePage.logoutMenuItem).toBeVisible();
  145 |     });
  146 | 
  147 |     test('Verify Home dashboard is restored after returning from another page', async ({ pageManager, page }) => {
  148 |         await pageManager.homePage.homePageLogo.waitFor({ state: 'visible', timeout: 20000 });
  149 | 
  150 |         await pageManager.homePage.navigateToIssuedChecks();
  151 |         await expect(page).toHaveURL(/\/issued-checks/, { timeout: 15000 });
  152 | 
  153 |         await pageManager.homePage.homePageLogo.click();
  154 |         await expect(page).toHaveURL(/\/$/, { timeout: 15000 });
  155 |         await expect(pageManager.homePage.combinedViewTab).toBeVisible();
  156 |         await expect(pageManager.homePage.exceptionsChartHeading).toBeVisible();
  157 |     });
  158 | 
  159 |     // ── Navigation from dashboard ──────────────────────────────────────────────
  160 | 
  161 |     test('Verify navigation to Issued Checks via nav menu', async ({ pageManager, page }) => {
  162 |         await pageManager.homePage.homePageLogo.waitFor({ state: 'visible', timeout: 20000 });
  163 |         await pageManager.homePage.navigateToIssuedChecks();
  164 |         await expect(page).toHaveURL(/\/issued-checks/, { timeout: 15000 });
  165 |     });
  166 | 
  167 |     test('Verify navigation to Presented Checks via nav menu', async ({ pageManager, page }) => {
  168 |         await pageManager.homePage.homePageLogo.waitFor({ state: 'visible', timeout: 20000 });
  169 |         await pageManager.homePage.navigateToPresentedChecks();
  170 |         await expect(page).toHaveURL(/\/paid-checks/, { timeout: 15000 });
  171 |     });
  172 | 
  173 |     test('Verify navigation to ACH via nav menu', async ({ pageManager, page }) => {
  174 |         await pageManager.homePage.homePageLogo.waitFor({ state: 'visible', timeout: 20000 });
  175 |         await pageManager.homePage.navigateToACH();
  176 |         await expect(page).toHaveURL(/\/paid-ach/, { timeout: 15000 });
  177 |     });
  178 | 
  179 |     test('Verify navigation to Exceptions via nav menu', async ({ pageManager, page }) => {
  180 |         await pageManager.homePage.homePageLogo.waitFor({ state: 'visible', timeout: 20000 });
```