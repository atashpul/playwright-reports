# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: specs\Smoke\7-ACHSmoke.spec.ts >> ACH — Smoke >> Search box filters grid and clears correctly
- Location: specs\Smoke\7-ACHSmoke.spec.ts:59:5

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: locator('tr.k-master-row.k-table-row').first()
Expected: visible
Timeout: 15000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 15000ms
  - waiting for locator('tr.k-master-row.k-table-row').first()

```

```yaml
- banner:
  - link:
    - /url: /
    - img
  - link "  Home":
    - /url: /
  - link "  Issued Checks":
    - /url: /issued-checks
  - link "  Presented Checks":
    - /url: /paid-checks
  - link "  Teller":
    - /url: /teller
  - link "  ACH":
    - /url: /paid-ach
  - link "  Exceptions":
    - /url: /exceptions
  - link "  Settings":
    - /url: /client-admin
  - button "  F.I ACH&CHK User"
- list:
  - listitem:
    - link " ":
      - /url: /
  - listitem:
    - link "ACH":
      - /url: /paid-ach
- group:
  - link " Uploaded ACH":
    - /url: /paid-ach/uploads
  - link "+ Add ACH":
    - /url: /paid-ach/add-ach
- toolbar "Grid toolbar":
  - text: Page Size
  - combobox:
    - option "10"
    - option "20" [selected]
    - option "50"
    - option "100"
  - button "Excel"
  - textbox "Search..."
  - text: Saved Filters
  - combobox:
    - option "[No Filter]" [selected]
  - button "+"
- grid "Data table":
  - rowgroup:
    - 'row "ID ID column filter menu settings Status Status column filter menu settings Decision Decision column filter menu settings Upload ID Upload ID column filter menu settings Added By Added By column filter menu settings Business Client Business Client column filter menu settings Account Account column filter menu settings Originator Originator column filter menu settings Originator Routing # Originator Routing # column filter menu settings Company ID Company ID column filter menu settings Amount Amount column filter menu settings SEC Code SEC Code column filter menu settings Trans Code Trans Code column filter menu settings Trans Type Trans Type column filter menu settings Entry Date  Entry Date column filter menu settings Settlement Date Settlement Date column filter menu settings Transaction ID Transaction ID column filter menu settings Edit Change Log"':
      - columnheader
      - columnheader "ID ID column filter menu settings":
        - text: ID
        - button "ID column filter menu settings"
      - columnheader "Status Status column filter menu settings":
        - text: Status
        - button "Status column filter menu settings"
      - columnheader "Decision Decision column filter menu settings":
        - text: Decision
        - button "Decision column filter menu settings"
      - columnheader "Upload ID Upload ID column filter menu settings":
        - text: Upload ID
        - button "Upload ID column filter menu settings"
      - columnheader "Added By Added By column filter menu settings":
        - text: Added By
        - button "Added By column filter menu settings"
      - columnheader "Business Client Business Client column filter menu settings":
        - text: Business Client
        - button "Business Client column filter menu settings"
      - columnheader "Account Account column filter menu settings":
        - text: Account
        - button "Account column filter menu settings"
      - columnheader "Originator Originator column filter menu settings":
        - text: Originator
        - button "Originator column filter menu settings"
      - 'columnheader "Originator Routing # Originator Routing # column filter menu settings"':
        - text: "Originator Routing #"
        - 'button "Originator Routing # column filter menu settings"'
      - columnheader "Company ID Company ID column filter menu settings":
        - text: Company ID
        - button "Company ID column filter menu settings"
      - columnheader "Amount Amount column filter menu settings":
        - text: Amount
        - button "Amount column filter menu settings"
      - columnheader "SEC Code SEC Code column filter menu settings":
        - text: SEC Code
        - button "SEC Code column filter menu settings"
      - columnheader "Trans Code Trans Code column filter menu settings":
        - text: Trans Code
        - button "Trans Code column filter menu settings"
      - columnheader "Trans Type Trans Type column filter menu settings":
        - text: Trans Type
        - button "Trans Type column filter menu settings"
      - columnheader "Entry Date  Entry Date column filter menu settings":
        - text: Entry Date 
        - button "Entry Date column filter menu settings"
      - columnheader "Settlement Date Settlement Date column filter menu settings":
        - text: Settlement Date
        - button "Settlement Date column filter menu settings"
      - columnheader "Transaction ID Transaction ID column filter menu settings":
        - text: Transaction ID
        - button "Transaction ID column filter menu settings"
      - columnheader "Edit"
      - columnheader "Change Log"
  - rowgroup:
    - row "No records available."
- application "Page navigation, page 1 of 0":
  - button "Go to the first page" [disabled]
  - button "Go to the previous page" [disabled]
  - text: Page
  - spinbutton "Select a page": "1"
  - text: of 0
  - button "Go to the next page" [disabled]
  - button "Go to the last page" [disabled]
  - text: 0 - 0 of 0 items
- contentinfo:
  - text: PJ_FI_Bank (Playwright Automation)[uat-release] © Copyright Advanced Fraud Solutions 2005-2026 | All Rights Reserved |
  - link "Privacy Policy":
    - /url: https://portal.advancedfraudsolutions.com/Help/PrivacyPolicy
```

# Test source

```ts
  1   | import { test, expect } from '../../utility/fixture';
  2   | import Constants from '../../utility/Constants';
  3   | import { LogoutPage } from '../../page-object/Login/Logout';
  4   | import { purgeSavedFilterPresets } from '../../utility/savedFilters';
  5   | 
  6   | test.describe('ACH — Smoke', () => {
  7   | 
  8   |     test.beforeEach(async ({ pageManager }) => {
  9   |         await pageManager.loginPage.navigateToLoginPage('/auth/login');
  10  |         await pageManager.loginPage.login(
  11  |             Constants.USER_FI_EMAIL,
  12  |             Constants.USER_FI_PASSWORD
  13  |         );
  14  |         await pageManager.homePage.homePageLogo.waitFor({ state: 'visible', timeout: 20000 });
  15  |         await pageManager.achPage.navigateToACH();
  16  |         await pageManager.achPage.waitForGridLoad();
  17  |     });
  18  | 
  19  |     // Safety net: the default-filter test creates an account-global Default preset, and the
  20  |     // save-preset test a non-default one. afterEach removes every SMK_ preset if an assertion
  21  |     // fails before its inline delete, so leftovers can't poison later runs.
  22  |     let pendingFilterPresetCleanup: string | undefined;
  23  | 
  24  |     test.afterEach(async ({ page, pageManager }) => {
  25  |         if (!pendingFilterPresetCleanup) return;
  26  |         pendingFilterPresetCleanup = undefined;
  27  | 
  28  |         await pageManager.achPage.navigateToACH();
  29  |         await purgeSavedFilterPresets(page, {
  30  |             dropdown: pageManager.achPage.savedFiltersDropdown,
  31  |             deleteButton: pageManager.achPage.deleteSavedFilterButton,
  32  |             confirmDialog: pageManager.achPage.confirmDialog,
  33  |             confirmButton: pageManager.achPage.confirmDialogConfirmButton,
  34  |         });
  35  |     });
  36  | 
  37  |     // ── Grid ────────────────────────────────────────────────────────────────
  38  | 
  39  |     test('Grid loads with data rows and key column headers', async ({ pageManager }) => {
  40  |         await expect(pageManager.achPage.gridRows.first()).toBeVisible({ timeout: 15000 });
  41  |         await pageManager.achPage.verifyGridColumnsVisible();
  42  |     });
  43  | 
  44  |     // ── Navigation ───────────────────────────────────────────────────────────
  45  | 
  46  |     test('"+ Add ACH" navigates to the add-ACH form', async ({ pageManager, page }) => {
  47  |         await pageManager.achPage.addACHButton.click();
  48  |         await expect(page).toHaveURL(/\/paid-ach\/add-ach/, { timeout: 15000 });
  49  |         await expect(pageManager.achPage.addACHBreadcrumb).toBeVisible({ timeout: 15000 });
  50  |     });
  51  | 
  52  |     test('"Uploaded ACH" navigates to the uploads page', async ({ page }) => {
  53  |         await page.getByRole('link', { name: /Uploaded ACH/i }).first().click();
  54  |         await expect(page).toHaveURL(/\/paid-ach\/uploads/, { timeout: 15000 });
  55  |     });
  56  | 
  57  |     // ── Toolbar ──────────────────────────────────────────────────────────────
  58  | 
  59  |     test('Search box filters grid and clears correctly', async ({ pageManager }) => {
  60  |         await pageManager.achPage.searchFor('__SMOKE_NO_RESULTS__');
  61  |         // Rows also vanish during Kendo's loading flash, so an empty grid is not proof the
  62  |         // search round-trip finished. The pager total is — and clearing while the search is
  63  |         // still in flight lets the two responses land out of order, leaving the grid empty.
  64  |         await expect(pageManager.achPage.paginationInfo).toContainText('0 - 0 of 0', { timeout: 15000 });
  65  |         await expect(pageManager.achPage.gridRows.first()).not.toBeVisible({ timeout: 15000 });
  66  |         await pageManager.achPage.clearSearch();
> 67  |         await expect(pageManager.achPage.gridRows.first()).toBeVisible({ timeout: 15000 });
      |                                                            ^ Error: expect(locator).toBeVisible() failed
  68  |     });
  69  | 
  70  |     test('Excel export button is visible and enabled', async ({ pageManager }) => {
  71  |         await expect(pageManager.achPage.excelExportButton).toBeVisible();
  72  |         await expect(pageManager.achPage.excelExportButton).toBeEnabled();
  73  |     });
  74  | 
  75  |     // ── Pagination ───────────────────────────────────────────────────────────
  76  | 
  77  |     test('Page size change to 10 updates pagination info in bottom-right pager', async ({ pageManager }) => {
  78  |         await pageManager.achPage.pageSizeDropdown.selectOption('10');
  79  |         // pager shows "1 - 10 of N items" after the grid reloads
  80  |         await expect(pageManager.achPage.paginationInfo).toContainText('1 - 10', { timeout: 15000 });
  81  |     });
  82  | 
  83  |     test('Next page arrow advances to page 2', async ({ pageManager }) => {
  84  |         await pageManager.achPage.pageSizeDropdown.selectOption('20');
  85  |         await expect(pageManager.achPage.paginationInfo).toContainText('1 - 20', { timeout: 15000 });
  86  |         await pageManager.achPage.nextPageButton.click();
  87  |         await expect(pageManager.achPage.paginationInfo).toContainText('21 -', { timeout: 15000 });
  88  |     });
  89  | 
  90  |     test('Last page arrow jumps to the final page', async ({ pageManager }) => {
  91  |         await pageManager.achPage.lastPageButton.click();
  92  |         // on the last page both forward arrows must be disabled
  93  |         await expect(pageManager.achPage.nextPageButton).toBeDisabled({ timeout: 15000 });
  94  |         await expect(pageManager.achPage.lastPageButton).toBeDisabled({ timeout: 15000 });
  95  |     });
  96  | 
  97  |     test('Typing a page number navigates to that page', async ({ pageManager }) => {
  98  |         await pageManager.achPage.pageSizeDropdown.selectOption('20');
  99  |         await expect(pageManager.achPage.paginationInfo).toContainText('1 - 20', { timeout: 15000 });
  100 | 
  101 |         const pagerText = (await pageManager.achPage.paginationInfo.textContent()) ?? '';
  102 |         const totalItemsMatch = pagerText.match(/of\s+(\d+)\s+items/i);
  103 |         const totalItems = totalItemsMatch ? Number(totalItemsMatch[1]) : 20;
  104 |         const pageSize = 20;
  105 |         const totalPages = Math.max(1, Math.ceil(totalItems / pageSize));
  106 |         const targetPage = Math.min(3, totalPages);
  107 |         const expectedStart = (targetPage - 1) * pageSize + 1;
  108 | 
  109 |         await pageManager.achPage.currentPageInput.click();
  110 |         await pageManager.achPage.currentPageInput.press('ControlOrMeta+a');
  111 |         await pageManager.achPage.currentPageInput.type(String(targetPage));
  112 |         await pageManager.achPage.currentPageInput.press('Enter');
  113 |         await expect(pageManager.achPage.paginationInfo).toContainText(`${expectedStart} -`, { timeout: 15000 });
  114 |     });
  115 | 
  116 |     test('Previous page button returns to page 1, First page button re-enables disabled state', async ({ pageManager }) => {
  117 |         await pageManager.achPage.pageSizeDropdown.selectOption('20');
  118 |         await expect(pageManager.achPage.paginationInfo).toContainText('1 - 20', { timeout: 15000 });
  119 | 
  120 |         await pageManager.achPage.nextPageButton.click();
  121 |         await expect(pageManager.achPage.paginationInfo).toContainText('21 -', { timeout: 15000 });
  122 | 
  123 |         // Previous returns to page 1
  124 |         await pageManager.achPage.previousPageButton.click();
  125 |         await expect(pageManager.achPage.paginationInfo).toContainText('1 - 20', { timeout: 15000 });
  126 | 
  127 |         // Jump to page 3 when available (otherwise the last available page), then return via First.
  128 |         const pagerText = (await pageManager.achPage.paginationInfo.textContent()) ?? '';
  129 |         const totalItemsMatch = pagerText.match(/of\s+(\d+)\s+items/i);
  130 |         const totalItems = totalItemsMatch ? Number(totalItemsMatch[1]) : 20;
  131 |         const pageSize = 20;
  132 |         const totalPages = Math.max(1, Math.ceil(totalItems / pageSize));
  133 |         const targetPage = Math.min(3, totalPages);
  134 |         const expectedStart = (targetPage - 1) * pageSize + 1;
  135 | 
  136 |         await pageManager.achPage.currentPageInput.click();
  137 |         await pageManager.achPage.currentPageInput.press('ControlOrMeta+a');
  138 |         await pageManager.achPage.currentPageInput.type(String(targetPage));
  139 |         await pageManager.achPage.currentPageInput.press('Enter');
  140 |         await expect(pageManager.achPage.paginationInfo).toContainText(`${expectedStart} -`, { timeout: 15000 });
  141 |         await pageManager.achPage.firstPageButton.click();
  142 |         await expect(pageManager.achPage.paginationInfo).toContainText('1 - 20', { timeout: 15000 });
  143 |         await expect(pageManager.achPage.previousPageButton).toBeDisabled({ timeout: 10000 });
  144 |         await expect(pageManager.achPage.firstPageButton).toBeDisabled({ timeout: 10000 });
  145 |     });
  146 | 
  147 |     // ── Saved Filters ────────────────────────────────────────────────────────
  148 | 
  149 |     test('Saved Filters dropdown is visible and defaults to no preset', async ({ pageManager }) => {
  150 |         await expect(pageManager.achPage.savedFiltersDropdown).toBeVisible();
  151 |         await expect(pageManager.achPage.savedFiltersDropdown).toHaveValue('');
  152 |     });
  153 | 
  154 |     test('Default filter auto-applies after logout and re-login', async ({ pageManager, page }, testInfo) => {
  155 |         // The default filter is account-global and shared by every browser on the same login.
  156 |         // Run on one browser only so concurrent projects can't collide on it (local 4-browser
  157 |         // runs and the pipeline's 4 parallel browser jobs share the same account).
  158 |         test.skip(testInfo.project.name !== 'chromium', 'Shared-account default filter runs on chromium only.');
  159 |         const logoutPage = new LogoutPage(page);
  160 | 
  161 |         // Save current grid state as default preset and verify it auto-applies after re-login.
  162 |         const baselineCount = await pageManager.achPage.gridRows.count();
  163 | 
  164 |         // Save the active filter as a named preset and mark it as Default.
  165 |         const presetName = `SMK_Default_${Date.now()}`;
  166 |         await pageManager.achPage.saveCurrentFilterAs(presetName, { asDefault: true });
  167 |         // Default preset now exists on the shared env — afterEach removes it even if we fail below.
```